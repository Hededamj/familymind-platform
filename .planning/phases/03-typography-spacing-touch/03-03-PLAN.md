---
phase: 03-typography-spacing-touch
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/dashboard/_components/recommendation-section.tsx
  - app/dashboard/_components/course-progress-card.tsx
  - components/community-pills.tsx
autonomous: true
requirements:
  - TOUCH-01
  - TOUCH-02

must_haves:
  truths:
    - "Recommendation card CTA buttons are at least 44px tall"
    - "Course progress card Continue/View buttons are at least 44px tall"
    - "Community pill links are at least 44px tall (comfortable thumb tap)"
    - "Horizontal scroll areas (none currently exist in app) would show no scrollbar — CSS utility is in place"
  artifacts:
    - path: "app/dashboard/_components/recommendation-section.tsx"
      provides: "Recommendation buttons with min-h-[44px]"
      contains: "min-h-[44px]"
    - path: "app/dashboard/_components/course-progress-card.tsx"
      provides: "Course progress buttons with min-h-[44px]"
      contains: "min-h-[44px]"
    - path: "components/community-pills.tsx"
      provides: "Community pill links with min-h-[44px]"
      contains: "min-h-[44px]"
  key_links:
    - from: "components/community-pills.tsx"
      to: "Link elements (community room pills)"
      via: "min-h-[44px] class"
      pattern: "min-h-\\[44px\\]"
    - from: "app/dashboard/_components/recommendation-section.tsx"
      to: "Button (CTA per recommendation)"
      via: "min-h-[44px] added to className"
      pattern: "min-h-\\[44px\\]"
---

<objective>
Add `min-h-[44px]` to interactive elements that are smaller than the 44px iOS/Android touch target minimum.

Purpose: Buttons with `size="sm"` are `h-8` (32px) and buttons with `size="lg"` are `h-10` (40px) — both below 44px. Community pill links at ~26px are clearly under the threshold. Adding `min-h-[44px]` without changing visual sizing ensures the tap area meets the standard.
Output: Three updated files with 44px minimum touch height on key interactive elements.

TOUCH-02 note: There are currently no horizontal scroll areas in the app (community pills were changed from scroll to flex-wrap). The `scrollbar-none` CSS utility in globals.css is available for future use — no file changes needed to satisfy TOUCH-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add min-h-[44px] to dashboard recommendation and course progress buttons</name>
  <files>
    app/dashboard/_components/recommendation-section.tsx,
    app/dashboard/_components/course-progress-card.tsx
  </files>
  <read_first>
    app/dashboard/_components/recommendation-section.tsx lines 64-70
    app/dashboard/_components/course-progress-card.tsx lines 56-68
  </read_first>
  <action>
    Per TOUCH-01 — three Button changes across two files:

    FILE 1: app/dashboard/_components/recommendation-section.tsx
    CTA Button (around line 66):
    Current: `<Button asChild variant="outline" className="w-full" size="sm">`
    New:     `<Button asChild variant="outline" className="w-full min-h-[44px]" size="sm">`

    FILE 2: app/dashboard/_components/course-progress-card.tsx
    Continue Button (around line 58):
    Current: `<Button asChild className="w-full" size="sm">`
    New:     `<Button asChild className="w-full min-h-[44px]" size="sm">`

    View course Button (around line 65):
    Current: `<Button asChild variant="outline" className="w-full" size="sm">`
    New:     `<Button asChild variant="outline" className="w-full min-h-[44px]" size="sm">`

    Do not touch anything else in either file.
  </action>
  <verify>
    <automated>grep -n "min-h-\[44px\]" app/dashboard/_components/recommendation-section.tsx app/dashboard/_components/course-progress-card.tsx</automated>
  </verify>
  <acceptance_criteria>recommendation-section.tsx has 1 match, course-progress-card.tsx has 2 matches for min-h-[44px].</acceptance_criteria>
  <done>grep returns 3 total lines across the two files</done>
</task>

<task type="auto">
  <name>Task 2: Add min-h-[44px] to community pill links</name>
  <files>components/community-pills.tsx</files>
  <read_first>components/community-pills.tsx lines 32-42</read_first>
  <action>
    Per TOUCH-01 — community pill Link elements are ~26px tall and are interactive links used on the dashboard.

    Link element (around line 33):
    Current: `className="flex items-center gap-1.5 rounded-full border border-[var(--color-border)] bg-white px-3 py-1.5 hover:bg-[var(--color-sand)] hover:text-[var(--foreground)]"`
    New:     `className="flex min-h-[44px] items-center gap-1.5 rounded-full border border-[var(--color-border)] bg-white px-3 py-1.5 hover:bg-[var(--color-sand)] hover:text-[var(--foreground)]"`

    Do not touch anything else in the file.
  </action>
  <verify>
    <automated>grep -n "min-h-\[44px\]" components/community-pills.tsx</automated>
  </verify>
  <acceptance_criteria>One match for min-h-[44px] on the Link className in community-pills.tsx.</acceptance_criteria>
  <done>grep returns 1 line</done>
</task>

</tasks>

<verification>
- `grep -n "min-h-\[44px\]" app/dashboard/_components/recommendation-section.tsx` returns 1 match
- `grep -n "min-h-\[44px\]" app/dashboard/_components/course-progress-card.tsx` returns 2 matches
- `grep -n "min-h-\[44px\]" components/community-pills.tsx` returns 1 match
- `grep -n "scrollbar-none" app/globals.css` returns match (CSS utility already present — TOUCH-02 satisfied)
- `npx tsc --noEmit` exits 0
</verification>

<success_criteria>
1. Recommendation card CTA buttons have min-h-[44px] on their className
2. Course progress card Continue and View buttons both have min-h-[44px]
3. Community pill links have min-h-[44px]
4. TOUCH-02: scrollbar-none CSS utility confirmed in globals.css — no horizontal scroll areas currently exist in the app
</success_criteria>

<output>
After completion, create `.planning/phases/03-typography-spacing-touch/03-03-SUMMARY.md`
</output>
