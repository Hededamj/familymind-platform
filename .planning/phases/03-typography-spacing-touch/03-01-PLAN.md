---
phase: 03-typography-spacing-touch
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/page.tsx
  - app/dashboard/progress/page.tsx
autonomous: true
requirements:
  - TYPO-01
  - TYPO-02
  - TYPO-04

must_haves:
  truths:
    - "Landing page hero h1 scales from readable mobile size to large desktop headline"
    - "Progress page h1 is not fixed-size — it scales between mobile and desktop"
    - "Progress page section h2s scale proportionally to avoid being too large on mobile"
    - "All main app pages have px-4 mobile and sm:px-8 desktop padding (verified, no changes needed)"
  artifacts:
    - path: "app/page.tsx"
      provides: "Landing hero with responsive font size"
      contains: "text-2xl sm:text-4xl lg:text-5xl"
    - path: "app/dashboard/progress/page.tsx"
      provides: "Progress page with responsive headings"
      contains: "text-2xl sm:text-3xl"
  key_links:
    - from: "app/page.tsx"
      to: "h1.heroHeading"
      via: "Tailwind responsive class stack"
      pattern: "text-2xl sm:text-4xl lg:text-5xl"
    - from: "app/dashboard/progress/page.tsx"
      to: "h1, h2 headings"
      via: "Tailwind responsive class stack"
      pattern: "text-2xl sm:text-3xl"
---

<objective>
Make hero and progress headings scale responsively between mobile and desktop.

Purpose: On a 375px mobile screen, the current hero `text-4xl` (36px) and progress `text-3xl` (30px) are fixed — too large at small sizes and don't scale to larger desktops. Responsive class stacks give appropriate sizing at every viewport.
Output: Two updated files with responsive heading classes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix landing page hero heading responsive size</name>
  <files>app/page.tsx</files>
  <read_first>app/page.tsx lines 48-56</read_first>
  <action>
    In the hero section (around line 52), change the `h1` font size class stack.

    Current: `className="font-serif text-4xl leading-tight sm:text-5xl"`
    New:     `className="font-serif text-2xl leading-tight sm:text-4xl lg:text-5xl"`

    This is a single class change. Do not touch any other part of the file.
    Per TYPO-01: responsive font sizes text-2xl to text-5xl.
  </action>
  <verify>
    <automated>grep -n "text-2xl.*sm:text-4xl.*lg:text-5xl" app/page.tsx</automated>
  </verify>
  <acceptance_criteria>The h1 in the hero section has class "text-2xl sm:text-4xl lg:text-5xl". No other changes in the file.</acceptance_criteria>
  <done>grep returns line with all three responsive classes on the hero h1</done>
</task>

<task type="auto">
  <name>Task 2: Fix progress page heading responsive sizes</name>
  <files>app/dashboard/progress/page.tsx</files>
  <read_first>app/dashboard/progress/page.tsx lines 36-42 and lines 84-90 and lines 124-130</read_first>
  <action>
    Make three class changes in this file (per TYPO-04):

    1. Page h1 (around line 36):
       Current: `className="mb-1 font-serif text-3xl"`
       New:     `className="mb-1 font-serif text-2xl sm:text-3xl"`

    2. Milestones section h2 (around line 86):
       Current: `className="mb-4 font-serif text-xl"`
       New:     `className="mb-4 font-serif text-lg sm:text-xl"`

    3. Monthly breakdown h2 (around line 126):
       Current: `className="mb-4 font-serif text-xl"`
       New:     `className="mb-4 font-serif text-lg sm:text-xl"`

    Do not touch anything else in the file.
  </action>
  <verify>
    <automated>grep -n "text-2xl sm:text-3xl\|text-lg sm:text-xl" app/dashboard/progress/page.tsx</automated>
  </verify>
  <acceptance_criteria>Three lines updated: h1 has "text-2xl sm:text-3xl", both h2s have "text-lg sm:text-xl".</acceptance_criteria>
  <done>grep returns 3 matches — one for h1 and two for h2 headings</done>
</task>

</tasks>

<verification>
- `grep -n "text-2xl sm:text-4xl lg:text-5xl" app/page.tsx` returns a match on the hero h1
- `grep -n "text-2xl sm:text-3xl" app/dashboard/progress/page.tsx` returns a match
- `grep -n "text-lg sm:text-xl" app/dashboard/progress/page.tsx` returns 2 matches
- `npx tsc --noEmit` exits 0 (no type errors introduced)
</verification>

<success_criteria>
1. Hero h1 class is "text-2xl sm:text-4xl lg:text-5xl" (was fixed text-4xl)
2. Progress h1 class is "text-2xl sm:text-3xl" (was fixed text-3xl)
3. Progress section h2s are "text-lg sm:text-xl" (were fixed text-xl)
4. All main app pages already have px-4 / sm:px-8 — TYPO-02 is satisfied without code changes
</success_criteria>

<output>
After completion, create `.planning/phases/03-typography-spacing-touch/03-01-SUMMARY.md`
</output>
