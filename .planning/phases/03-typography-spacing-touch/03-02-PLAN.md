---
phase: 03-typography-spacing-touch
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/community/[roomSlug]/[postSlug]/page.tsx
  - app/community/page.tsx
  - app/community/[roomSlug]/page.tsx
autonomous: true
requirements:
  - TYPO-03
  - TOUCH-03

must_haves:
  truths:
    - "A community post body with a long unbroken word (e.g. URL or long Danish compound) wraps within its container"
    - "Reply bodies in the single post view also wrap safely"
    - "Community room names in the room grid are clamped to 1 line"
    - "Post body previews in the room feed are clamped to 2 lines"
  artifacts:
    - path: "app/community/[roomSlug]/[postSlug]/page.tsx"
      provides: "Post and reply bodies with break-words and hyphens"
      contains: "break-words hyphens-auto"
    - path: "app/community/page.tsx"
      provides: "Room name with line-clamp"
      contains: "line-clamp-1"
    - path: "app/community/[roomSlug]/page.tsx"
      provides: "Post preview body with line-clamp"
      contains: "line-clamp-2"
  key_links:
    - from: "app/community/[roomSlug]/[postSlug]/page.tsx"
      to: "post.body and reply.body paragraphs"
      via: "Tailwind utility classes"
      pattern: "break-words hyphens-auto"
---

<objective>
Add text-overflow protection to community content: wrap long words in post and reply bodies, clamp room names and post previews in card views.

Purpose: User-generated content can contain long unbroken strings (URLs, compound words, no-space text). Without `break-words` and `hyphens-auto`, these overflow their container causing horizontal scroll. Card titles need `line-clamp` to prevent multi-line overflow in grid layouts.
Output: Three updated community files with safe text handling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add break-words and hyphens to post and reply bodies</name>
  <files>app/community/[roomSlug]/[postSlug]/page.tsx</files>
  <read_first>app/community/[roomSlug]/[postSlug]/page.tsx lines 92-98 and lines 130-135</read_first>
  <action>
    Two class additions in this file (per TYPO-03):

    1. Post body paragraph (around line 94):
       Current: `className="whitespace-pre-wrap text-sm leading-relaxed text-foreground/90 sm:text-base"`
       New:     `className="whitespace-pre-wrap break-words hyphens-auto text-sm leading-relaxed text-foreground/90 sm:text-base"`

    2. Reply body paragraph (around line 132):
       Current: `className="whitespace-pre-wrap text-sm leading-relaxed text-foreground/90"`
       New:     `className="whitespace-pre-wrap break-words hyphens-auto text-sm leading-relaxed text-foreground/90"`

    Do not touch anything else in the file. Note: `hyphens-auto` maps to `hyphens: auto` CSS — works with `lang="da"` on the html element (set in root layout).
  </action>
  <verify>
    <automated>grep -c "break-words hyphens-auto" "app/community/[roomSlug]/[postSlug]/page.tsx"</automated>
  </verify>
  <acceptance_criteria>Both post body and reply body paragraphs have "break-words hyphens-auto" added to their className. grep -c returns 2.</acceptance_criteria>
  <done>Two occurrences of "break-words hyphens-auto" found in the file</done>
</task>

<task type="auto">
  <name>Task 2: Add line-clamp to community room cards and post feed previews</name>
  <files>app/community/page.tsx, app/community/[roomSlug]/page.tsx</files>
  <read_first>
    app/community/page.tsx lines 55-60
    app/community/[roomSlug]/page.tsx lines 119-125
  </read_first>
  <action>
    Two changes across two files (per TOUCH-03):

    FILE 1: app/community/page.tsx
    Room name h2 (around line 57):
    Current: `className="font-serif text-lg text-foreground group-hover:text-accent-foreground"`
    New:     `className="font-serif text-lg text-foreground group-hover:text-accent-foreground line-clamp-1"`

    FILE 2: app/community/[roomSlug]/page.tsx
    Post body preview paragraph (around line 122):
    Current: `className="text-sm leading-relaxed text-foreground/90"`
    New:     `className="text-sm leading-relaxed text-foreground/90 line-clamp-2"`

    Do not touch anything else in either file.
  </action>
  <verify>
    <automated>grep -n "line-clamp-1" "app/community/page.tsx" && grep -n "line-clamp-2" "app/community/[roomSlug]/page.tsx"</automated>
  </verify>
  <acceptance_criteria>community/page.tsx room name h2 has "line-clamp-1"; [roomSlug]/page.tsx post preview p has "line-clamp-2".</acceptance_criteria>
  <done>Both grep commands return matches</done>
</task>

</tasks>

<verification>
- `grep -c "break-words hyphens-auto" "app/community/[roomSlug]/[postSlug]/page.tsx"` returns 2
- `grep -n "line-clamp-1" "app/community/page.tsx"` returns a match on the room name h2
- `grep -n "line-clamp-2" "app/community/[roomSlug]/page.tsx"` returns a match on the body preview
- `npx tsc --noEmit` exits 0
</verification>

<success_criteria>
1. Post body and reply body in single-post page both have "break-words hyphens-auto"
2. Community room name in the grid card has "line-clamp-1"
3. Post feed body preview has "line-clamp-2"
</success_criteria>

<output>
After completion, create `.planning/phases/03-typography-spacing-touch/03-02-SUMMARY.md`
</output>
