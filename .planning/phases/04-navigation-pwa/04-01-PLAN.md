---
phase: 04-navigation-pwa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/subscribe/layout.tsx
  - app/content/layout.tsx
  - components/layout/topbar.tsx
  - components/layout/footer.tsx
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - NAV-03

must_haves:
  truths:
    - "Authenticated users visiting /subscribe see the bottom tab bar and sidebar"
    - "Authenticated users visiting /content/[slug] see the bottom tab bar and sidebar"
    - "The marketing topbar does not appear on /subscribe or /content/* for logged-in users"
    - "The marketing footer does not appear on /subscribe or /content/* regardless of auth state"
    - "Back button appears in the app topbar on /subscribe and /content/[slug]"
  artifacts:
    - path: "app/subscribe/layout.tsx"
      provides: "AppLayout wrapper for /subscribe route"
      contains: "AppLayout"
    - path: "app/content/layout.tsx"
      provides: "AppLayout wrapper for /content/* routes"
      contains: "AppLayout"
    - path: "components/layout/topbar.tsx"
      provides: "Marketing topbar hidden on all app routes when logged in"
      contains: "/subscribe"
    - path: "components/layout/footer.tsx"
      provides: "Marketing footer hidden on all app routes unconditionally"
      contains: "/subscribe"
  key_links:
    - from: "app/subscribe/layout.tsx"
      to: "components/layout/app-layout.tsx"
      via: "import AppLayout"
      pattern: "AppLayout"
    - from: "components/layout/topbar.tsx"
      to: "pathname.startsWith"
      via: "isLoggedIn condition"
      pattern: "/subscribe.*isLoggedIn"
---

<objective>
Add AppLayout wrappers to the two remaining authenticated app routes (/subscribe and /content) and extend the topbar/footer hide lists to cover all app routes.

Purpose: Every user-facing route must use the correct app shell (bottom tab bar + sidebar), and the marketing topbar/footer must not appear on app routes for authenticated users.
Output: app/subscribe/layout.tsx, app/content/layout.tsx (new), updated topbar.tsx and footer.tsx
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Existing pattern from app/browse/layout.tsx (copy this pattern exactly) -->
```tsx
import { AppLayout } from '@/components/layout/app-layout'

export default function BrowseLayout({ children }: { children: React.ReactNode }) {
  return <AppLayout>{children}</AppLayout>
}
```

<!-- AppLayout signature from components/layout/app-layout.tsx -->
```tsx
export async function AppLayout({ children }: { children: React.ReactNode })
// Returns children unwrapped if no authenticated user — safe to use on semi-public routes
```

<!-- AppTopbar back button logic (already correct, driven by topLevelRoutes array) -->
<!-- topLevelRoutes = ['/dashboard', '/community', '/browse'] -->
<!-- Any path NOT in that list shows the back button automatically — no changes needed to app-topbar.tsx -->

<!-- Current topbar hide conditions (topbar.tsx lines 43-49) -->
<!--
  if (pathname?.startsWith('/admin')) return null
  if (pathname?.startsWith('/dashboard')) return null
  if (pathname?.startsWith('/onboarding')) return null
  if (pathname?.startsWith('/journeys')) return null
  if (pathname?.startsWith('/browse') && isLoggedIn) return null
  if (pathname?.startsWith('/products') && isLoggedIn) return null
  if (pathname?.startsWith('/community') && isLoggedIn) return null
-->

<!-- Current footer hide conditions (footer.tsx lines 23-34) -->
<!--
  if (
    pathname?.startsWith('/admin') ||
    pathname?.startsWith('/dashboard') ||
    pathname?.startsWith('/onboarding') ||
    pathname?.startsWith('/journeys') ||
    pathname?.startsWith('/browse') ||
    pathname?.startsWith('/products') ||
    pathname?.startsWith('/community') ||
    (pathname?.includes('/day/'))
  ) return null
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create layout.tsx files for subscribe and content routes</name>
  <files>app/subscribe/layout.tsx, app/content/layout.tsx</files>
  <read_first>
    - app/browse/layout.tsx (copy this exact pattern)
    - app/subscribe/page.tsx (confirm route exists)
    - app/content/[slug]/page.tsx (confirm route exists)
  </read_first>
  <action>
    Create app/subscribe/layout.tsx:
    ```tsx
    import { AppLayout } from '@/components/layout/app-layout'

    export default function SubscribeLayout({ children }: { children: React.ReactNode }) {
      return <AppLayout>{children}</AppLayout>
    }
    ```

    Create app/content/layout.tsx:
    ```tsx
    import { AppLayout } from '@/components/layout/app-layout'

    export default function ContentLayout({ children }: { children: React.ReactNode }) {
      return <AppLayout>{children}</AppLayout>
    }
    ```

    Note: AppLayout already handles unauthenticated users by returning children unwrapped (line 13 of app-layout.tsx), so /subscribe and /content remain accessible publicly — the subscription page is semi-public and the content page has its own gating logic.

    Note: AppTopbar already shows a back button for any path not in topLevelRoutes (['/dashboard', '/community', '/browse']), so /subscribe and /content/[slug] will automatically get the back button (NAV-03 satisfied) without any change to app-topbar.tsx.
  </action>
  <verify>
    <automated>test -f C:/Users/jacob.hummel/Claude/familymind-platform/app/subscribe/layout.tsx && test -f C:/Users/jacob.hummel/Claude/familymind-platform/app/content/layout.tsx && echo "PASS: both layout files exist"</automated>
  </verify>
  <done>Both layout files exist with AppLayout import and default export wrapping children</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend topbar and footer hide lists to cover /subscribe and /content</name>
  <files>components/layout/topbar.tsx, components/layout/footer.tsx</files>
  <read_first>
    - components/layout/topbar.tsx (read full file — modify the early-return block)
    - components/layout/footer.tsx (read full file — modify the early-return block)
  </read_first>
  <action>
    In components/layout/topbar.tsx, add two lines after the existing early-return guards (after line 49, before the isAuthPage line):

    ```tsx
    if (pathname?.startsWith('/subscribe') && isLoggedIn) return null
    if (pathname?.startsWith('/content') && isLoggedIn) return null
    ```

    The full block should then read:
    ```tsx
    if (pathname?.startsWith('/admin')) return null
    if (pathname?.startsWith('/dashboard')) return null
    if (pathname?.startsWith('/onboarding')) return null
    if (pathname?.startsWith('/journeys')) return null
    if (pathname?.startsWith('/browse') && isLoggedIn) return null
    if (pathname?.startsWith('/products') && isLoggedIn) return null
    if (pathname?.startsWith('/community') && isLoggedIn) return null
    if (pathname?.startsWith('/subscribe') && isLoggedIn) return null
    if (pathname?.startsWith('/content') && isLoggedIn) return null
    ```

    Rationale for `&& isLoggedIn`: /subscribe and /content are semi-public. Non-logged-in users visiting /subscribe should still see the marketing topbar (to drive signups). AppLayout.tsx already handles returning children unwrapped when not logged in, so non-authenticated users fall through to the normal marketing shell.

    In components/layout/footer.tsx, add /subscribe and /content to the existing early-return OR-block:

    ```tsx
    if (
      pathname?.startsWith('/admin') ||
      pathname?.startsWith('/dashboard') ||
      pathname?.startsWith('/onboarding') ||
      pathname?.startsWith('/journeys') ||
      pathname?.startsWith('/browse') ||
      pathname?.startsWith('/products') ||
      pathname?.startsWith('/community') ||
      pathname?.startsWith('/subscribe') ||
      pathname?.startsWith('/content') ||
      (pathname?.includes('/day/'))
    ) {
      return null
    }
    ```

    Rationale for unconditional hide on footer: the subscribe page and content page have their own full-bleed layouts. The marketing footer is not appropriate on either page even for non-logged-in visitors.
  </action>
  <verify>
    <automated>grep -c "subscribe" C:/Users/jacob.hummel/Claude/familymind-platform/components/layout/topbar.tsx && grep -c "subscribe" C:/Users/jacob.hummel/Claude/familymind-platform/components/layout/footer.tsx</automated>
  </verify>
  <done>
    topbar.tsx hides on /subscribe and /content when isLoggedIn.
    footer.tsx hides on /subscribe and /content unconditionally.
    grep confirms "subscribe" appears in both files.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Run `npx tsc --noEmit` — no type errors in modified files
2. Confirm app/subscribe/layout.tsx and app/content/layout.tsx exist with correct imports
3. Confirm topbar.tsx contains lines for /subscribe and /content
4. Confirm footer.tsx contains /subscribe and /content in its guard block
</verification>

<success_criteria>
- app/subscribe/layout.tsx exists, exports AppLayout wrapper
- app/content/layout.tsx exists, exports AppLayout wrapper
- topbar.tsx hides marketing topbar on /subscribe and /content for authenticated users (NAV-02)
- footer.tsx hides marketing footer on /subscribe and /content always (NAV-02)
- Back button on sub-pages works because AppTopbar already handles it via topLevelRoutes (NAV-03)
- All app routes now have AppLayout — no missing wrappers (NAV-01)
</success_criteria>

<output>
After completion, create `.planning/phases/04-navigation-pwa/04-01-SUMMARY.md`
</output>
