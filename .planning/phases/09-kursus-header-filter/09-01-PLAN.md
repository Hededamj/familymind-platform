---
phase: 09-kursus-header-filter
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/courses/[slug]/page.tsx
autonomous: true
requirements: [COURSE-03]
must_haves:
  truths:
    - "When product.coverImageUrl is set, authenticated course header shows the cover image above the title"
    - "When coverImageUrl is absent, the header renders unchanged (no visual regression)"
    - "Cover image URL is sanitized (whitespace stripped) before being passed to next/image"
  artifacts:
    - path: "app/courses/[slug]/page.tsx"
      provides: "Cover image rendering inside authenticated header card"
      contains: "product.coverImageUrl"
  key_links:
    - from: "app/courses/[slug]/page.tsx (authenticated branch header)"
      to: "next/image"
      via: "<Image fill> inside aspect-[16/9] container"
      pattern: "aspect-\\[16/9\\]"
---

<objective>
Add a cover image to the authenticated course header card (COURSE-03). The unauthenticated landing branch already renders product.coverImageUrl; this plan mirrors the pattern inside the authenticated branch's header card using a full-bleed 16:9 image above the existing sand-gradient content area.

Purpose: Visual richness — users entering a course see the product's cover image as the first element, matching the landing-page branding.
Output: Modified app/courses/[slug]/page.tsx with cover image in authenticated header.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-kursus-header-filter/09-RESEARCH.md
@app/courses/[slug]/page.tsx

<interfaces>
From current page.tsx:
- `product.coverImageUrl`: `string | null` (already on Product, already used in landing branch lines 229-241)
- `next/image` already imported at line 2
- Sanitization pattern: `.replace(/\s+/g, '')` (used in resolveThumbnail line 16)
- Authenticated header card lives at lines 88-120: `<div className="mb-8 overflow-hidden rounded-2xl bg-gradient-to-br from-[var(--color-sand)] to-white p-5 sm:p-6">`
- next.config.ts whitelists `*.b-cdn.net` for Bunny CDN
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add cover image above sand-gradient header content</name>
  <files>app/courses/[slug]/page.tsx</files>
  <read_first>
    - Read app/courses/[slug]/page.tsx lines 88-120 (current authenticated header card structure)
    - Read app/courses/[slug]/page.tsx lines 229-241 (existing coverImageUrl usage in landing branch)
    - Confirm `import Image from 'next/image'` is already present at line 2
  </read_first>
  <action>
    Restructure the authenticated header card at lines 88-120 to include a cover image above the existing sand-gradient content area. The current structure is a single div with rounded-2xl + gradient + padding. Change it to a wrapper div with `overflow-hidden rounded-2xl` containing:
    1. A conditional `<div className="relative aspect-[16/9] w-full">` with `<Image fill>` when coverImageUrl exists
    2. The existing sand-gradient content (title, description, progress) moved into an inner `<div className="bg-gradient-to-br from-[var(--color-sand)] to-white p-5 sm:p-6">`

    Concrete replacement for lines 88-120:

    ```tsx
    {/* Course header card */}
    <div className="mb-8 overflow-hidden rounded-2xl">
      {product.coverImageUrl && (
        <div className="relative aspect-[16/9] w-full">
          <Image
            src={product.coverImageUrl.replace(/\s+/g, '')}
            alt={product.title}
            fill
            className="object-cover"
            sizes="(max-width: 640px) 100vw, 672px"
            priority
          />
        </div>
      )}
      <div className="bg-gradient-to-br from-[var(--color-sand)] to-white p-5 sm:p-6">
        <h1 className="mb-1.5 font-serif text-xl sm:text-2xl">{product.title}</h1>
        {product.description && (
          <p className="mb-4 text-sm leading-relaxed text-muted-foreground">{product.description}</p>
        )}

        {/* Progress section */}
        <div className="space-y-2">
          <div className="flex items-baseline justify-between">
            <span className="text-sm font-medium">
              {courseProgress.percentComplete}% gennemført
            </span>
            <span className="text-xs text-muted-foreground">
              {courseProgress.completedLessons} af {courseProgress.totalLessons} lektioner
            </span>
          </div>
          <div className="h-2 w-full overflow-hidden rounded-full bg-white">
            <div
              className="h-full rounded-full bg-[var(--color-coral)] transition-all duration-500"
              style={{ width: `${courseProgress.percentComplete}%` }}
            />
          </div>
          <div className="flex gap-3 text-xs text-muted-foreground">
            {courseProgress.chapterCount > 0 && (
              <span>{courseProgress.chapterCount} kapitler</span>
            )}
            {courseProgress.totalDurationMinutes > 0 && (
              <span>ca. {courseProgress.totalDurationMinutes} min</span>
            )}
          </div>
        </div>
      </div>
    </div>
    ```

    Use `.replace(/\s+/g, '')` sanitization to avoid the same whitespace bug that affects thumbnailUrl. Use `fill` + `sizes` (NOT width/height) so the 16:9 aspect container controls dimensions responsively. Keep `priority` because it's above-the-fold on authenticated course entry.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep: `product.coverImageUrl.replace` appears in app/courses/[slug]/page.tsx
    - grep: `aspect-\[16/9\]` appears in app/courses/[slug]/page.tsx
    - grep: `sizes="(max-width: 640px)` appears in authenticated header
    - grep: `priority` appears on the new Image
    - `npx tsc --noEmit` exits 0
    - The sand-gradient `<div className="bg-gradient-to-br from-[var(--color-sand)] to-white p-5 sm:p-6">` still contains the h1 with product.title
    - No duplication of the progress bar section
  </acceptance_criteria>
  <done>Authenticated course header renders cover image (when present) above the sand-gradient content area; header still renders correctly without cover image; tsc passes.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- Visual inspection: navigate to /courses/{slug} as authenticated user with access — cover image appears above title
- Visual inspection: course without coverImageUrl — header renders exactly as Phase 8 left it
</verification>

<success_criteria>
COURSE-03 complete: "Kursus-header viser cover-billede hvis tilgængeligt" — authenticated header shows cover when product.coverImageUrl is set, renders unchanged when absent.
</success_criteria>

<output>
After completion, create `.planning/phases/09-kursus-header-filter/09-01-SUMMARY.md`
</output>
