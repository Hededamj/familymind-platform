---
phase: 12-hygge-cancel-flow-ui
plan: 03
type: execute
wave: 3
depends_on: [12-02]
files_modified:
  - app/dashboard/subscription/cancel/_components/cancel-flow.tsx
  - app/dashboard/subscription/cancel/_components/step-offer-discount.tsx
  - app/dashboard/subscription/cancel/_components/step-offer-pause.tsx
  - app/dashboard/subscription/cancel/_components/step-offer-support.tsx
  - app/dashboard/subscription/cancel/_components/step-offer-content.tsx
  - app/dashboard/subscription/cancel/_components/step-offer-skip.tsx
  - app/dashboard/subscription/cancel/_components/step-confirm.tsx
  - app/dashboard/subscription/cancel/_components/step-done.tsx
autonomous: true
requirements: [OFF-UI-04, OFF-UI-05, OFF-UI-06, OFF-UI-07]
must_haves:
  truths:
    - "Each of the 5 offer variants renders when routed from reason step with matching EligibleOffer"
    - "DISCOUNT variant computes newPrice = round(priceAmountCents * 0.8) and hides price row if priceAmountCents is null"
    - "PAUSE variant disables primary CTA until a duration (1/2/3) is selected, passes months to acceptOfferAction"
    - "SUPPORT variant primary CTA opens supportUrl in new tab with mailto:support@familymind.nu fallback"
    - "CONTENT variant shows 3 hardcoded fallback cards (Søvn og rutiner / Følelsesregulering / Stille stunder)"
    - "Step 4 (confirm) shows formatted Danish periodEndDate and calls confirmCancelAction"
    - "Step 5 renders 3 variants (cancel / pause / retention) with correct personalized copy"
    - "All step components use design system tokens (sand, coral, DM Serif Display)"
  artifacts:
    - path: "app/dashboard/subscription/cancel/_components/step-offer-discount.tsx"
    - path: "app/dashboard/subscription/cancel/_components/step-offer-pause.tsx"
    - path: "app/dashboard/subscription/cancel/_components/step-offer-support.tsx"
    - path: "app/dashboard/subscription/cancel/_components/step-offer-content.tsx"
    - path: "app/dashboard/subscription/cancel/_components/step-offer-skip.tsx"
    - path: "app/dashboard/subscription/cancel/_components/step-confirm.tsx"
    - path: "app/dashboard/subscription/cancel/_components/step-done.tsx"
  key_links:
    - from: "cancel-flow.tsx"
      to: "all step-* components"
      via: "imports + conditional render on step state"
      pattern: "StepOffer|StepConfirm|StepDone"
---

<objective>
Complete the cancel flow by implementing all Step 3 offer variants (5 components), Step 4 confirm, and Step 5 done (3 variants as one component). Wire them into cancel-flow.tsx, replacing the Plan 12-02 placeholder block and removing the hidden-span hack.

Purpose: Ship the full hygge cancel flow with all copy verbatim from the mockup and all routing logic complete.
Output: Functional multi-step cancel flow that calls real services and navigates back to /dashboard on done.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-hygge-cancel-flow-ui/12-RESEARCH.md
@app/dashboard/subscription/cancel/_components/cancel-flow.tsx
@app/dashboard/subscription/cancel/_components/step-intro.tsx
@app/dashboard/subscription/cancel/_components/step-reason.tsx
@app/dashboard/subscription/cancel/actions.ts
@lib/services/retention.service.ts

<interfaces>
EligibleOffer type from retention.service.ts (see Plan 12-01 context).
CancelFlow state already has: surveyId, offerId, offerConfig, acceptanceExpiresAt, handleAcceptOffer(pauseMonths?), handleConfirmCancel, handleReturnToDashboard.

Danish date format: `date.toLocaleDateString('da-DK', { day: 'numeric', month: 'long', year: 'numeric' })` → "8. maj 2026".

Design tokens (same as Plan 12-02).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create all 7 step components with exact mockup copy</name>
  <files>app/dashboard/subscription/cancel/_components/step-offer-discount.tsx, app/dashboard/subscription/cancel/_components/step-offer-pause.tsx, app/dashboard/subscription/cancel/_components/step-offer-support.tsx, app/dashboard/subscription/cancel/_components/step-offer-content.tsx, app/dashboard/subscription/cancel/_components/step-offer-skip.tsx, app/dashboard/subscription/cancel/_components/step-confirm.tsx, app/dashboard/subscription/cancel/_components/step-done.tsx</files>
  <action>
All 7 files are `'use client'`. Use the following prop contracts and the exact Danish copy from 12-RESEARCH.md section 7 (copy is canonical — do not paraphrase).

Shared helper for formatting (inline in each file that needs it):
```typescript
const fmtDate = (d: Date) =>
  d.toLocaleDateString('da-DK', { day: 'numeric', month: 'long', year: 'numeric' })
```

**File 1: `step-offer-discount.tsx`**
Props: `{ offerConfig: EligibleOffer; priceAmountCents: number | null; loading: boolean; onAccept: () => void; onDecline: () => void; onBack: () => void }`

- Render a discount card at top: large "-20%" heading + "i {durationMonths} måneder" subtext. Use `style={{ background: 'linear-gradient(135deg, var(--color-coral) 0%, #F4A28C 100%)' }}` on a rounded-2xl div, text white.
- Inside card (if `priceAmountCents != null`): show `{priceAmountCents/100} kr/md` with `line-through opacity-70` and below `{Math.round(priceAmountCents * 0.8 / 100)} kr/md` bold. Hide this price row entirely when `priceAmountCents == null`.
- Heading: "Før du går — hvad med en lille gave?"
- Body: "Vi forstår godt at økonomien kan være stram. Du får 20% rabat de næste {offerConfig.durationMonths} måneder, så du kan fortsætte uden at tænke over det."
- Primary CTA: "Ja tak, det vil jeg gerne" → calls `onAccept()`, disabled while `loading`, shows "Gemmer…" when loading
- Decline link: "Nej tak, jeg vil alligevel opsige" → calls `onDecline()`
- Back chevron at top: "← Tilbage"

**File 2: `step-offer-pause.tsx`**
Props: `{ loading: boolean; onAccept: (months: 1|2|3) => void; onDecline: () => void; onBack: () => void }`

- Illustration: ☕ in a `bg-[var(--color-coral)]/10` circle
- Heading: "Vil du have en pause i stedet?"
- Body: "Nogle gange er det bare ikke det rigtige tidspunkt. Du kan sætte dit abonnement på pause — og være her igen når du er klar."
- Info box (sand bg, rounded-2xl, p-5): title "I pausen:" then list with ♡ bullet prefix:
  - "Du betaler ikke noget"
  - "Dine bogmærker og fremskridt gemmes"
  - "Du kan altid vende tilbage tidligere"
- Label: "Hvor længe?"
- Grid: `grid grid-cols-3 gap-2.5` with 3 buttons "1 måned" / "2 måneder" / "3 måneder". Use local `useState<1|2|3|null>` for selection. Selected button: coral border + coral bg-opacity-10.
- Primary CTA: "Pause mit abonnement" — disabled when `selected === null || loading`. onClick: `onAccept(selected!)`.
- Decline link: "Nej tak, jeg vil gerne opsige helt" → `onDecline()`

**File 3: `step-offer-support.tsx`**
Props: `{ offerConfig: EligibleOffer; onDecline: () => void; onBack: () => void }`

- Illustration: 🛠️ in a `bg-[var(--color-warm-blue)]/20` circle (fallback: `bg-slate-200`)
- Heading: "Lad os fikse det først"
- Body: "Tekniske problemer er altid vores skyld — og vi vil gerne gøre det godt igen. Skriv til os, og vi hjælper dig inden du beslutter dig."
- Support card (sand bg, rounded-2xl, p-5, flex items-center gap-4): ✉️ icon + title "Skriv til vores support" + subtext "Vi svarer inden for 24 timer på hverdage"
- Primary CTA: render as `<a>` element styled as primary button, `href={offerConfig.supportUrl ?? 'mailto:support@familymind.nu'}`, `target="_blank"`, `rel="noopener noreferrer"`, text "Kontakt support"
- Decline link: "Nej tak, jeg vil gerne opsige alligevel" → `onDecline()`

**File 4: `step-offer-content.tsx`**
Props: `{ onStay: () => void; onDecline: () => void; onBack: () => void }`

- Heading (left-aligned): "Har du set disse?"
- Subtext: "Nogle populære kurser du måske ikke har opdaget endnu. Du har stadig adgang til alt — måske er der noget her der passer bedre?"
- Hardcoded 3 content cards (Phase 12 decision — admin-configurable in Phase 13). Each card is `rounded-2xl border border-border bg-white p-4 flex items-center gap-4`:
  - 🌙 emoji + "Søvn og rutiner" + "Populært kursus om gode nattesøvn-vaner"
  - 💛 emoji + "Følelsesregulering" + "Håndter store følelser sammen med dit barn"
  - 🌱 emoji + "Stille stunder" + "Små øjeblikke der gør en stor forskel"
- Primary CTA: "Fortsæt mit abonnement" → `onStay()` (which navigates user back to /dashboard — NOT accept an offer, since no offer row is involved for CONTENT_HELP in the UI flow. Wire this in cancel-flow task 2.)
- Decline link: "Nej tak, jeg vil gerne opsige" → `onDecline()`

**File 5: `step-offer-skip.tsx`**
Props: `{ onContinue: () => void; onBack: () => void }`

- Illustration: 🌻 in a green-ish circle `bg-green-100`
- Heading: "Det er helt OK"
- Body: "Nogle gange er det bare tid til noget andet — og det er vi glade på dine vegne for. Tak fordi du var her."
- Info box (sand, rounded-2xl, p-5) with 3 items (♡ prefix):
  - "Vi respekterer dit valg"
  - "Din dør er altid åben"
  - "Alt er gemt hvis du vender tilbage"
- Primary CTA: "Fortsæt til bekræftelse" → `onContinue()`

**File 6: `step-confirm.tsx`**
Props: `{ periodEndDate: Date; loading: boolean; onConfirm: () => void; onGoBack: () => void; onBack: () => void }`

- Back chevron: "← Tilbage"
- Heading: "Er du sikker?"
- Body (use `<p>` with `<strong>` for the date): `Dit abonnement stopper den <strong>{fmtDate(periodEndDate)}</strong>. Indtil da har du fuld adgang — som du plejer.`
- Info box (sand, rounded-2xl, p-5) with 4 items:
  - "Du beholder adgang til {fmtDate(periodEndDate)}"
  - "Du bliver ikke opkrævet igen"
  - "Dine bogmærker og fremskridt gemmes"
  - "Du kan altid komme tilbage"
- Primary CTA: "Opsig abonnement" — disabled while loading, shows "Opsiger…" when loading → `onConfirm()`
- Decline link: "Nej, jeg vil beholde mit abonnement" → `onGoBack()` (navigates back to /dashboard)

**File 7: `step-done.tsx`**
Props: `{ variant: 'cancel' | 'pause' | 'retention'; userName: string; periodEndDate: Date; expiresAt: Date | null; offerConfig: EligibleOffer | null; onReturn: () => void }`

Render one of three variants based on `variant`:

*Cancel variant:*
- Illustration: 🏡 in `bg-gradient-to-br from-[var(--color-sand)] to-white` circle
- Heading: `Tak for din tid hos os, {userName}`
- Body paragraphs (each as separate `<p>`, with the last using `<strong>` around date):
  1. "Vi ønsker dig alt det bedste."
  2. "Din dør er altid åben hvis du vil tilbage — dit indhold, dine bogmærker og din rejse er gemt her."
  3. `Du har stadig adgang indtil <strong>{fmtDate(periodEndDate)}</strong>.`

*Pause variant:*
- Illustration: ⏸️ in `bg-[var(--color-coral)]/10` circle
- Heading: "Pause aktiveret"
- Body paragraphs:
  1. `Vi ses igen den <strong>{fmtDate(expiresAt!)}</strong>.` (expiresAt guaranteed non-null in pause variant)
  2. "Du bliver ikke opkrævet i mellemtiden, og alt er gemt som du efterlod det."
  3. `Hvis du vil tilbage tidligere, kan du genaktivere fra "Mit abonnement".`

*Retention (discount) variant:*
- Illustration: 🎁 in `bg-[var(--color-coral)]/10` circle
- Heading: `Dejligt at du bliver, {userName}`
- durationMonths from `offerConfig?.durationMonths ?? 3`
- Body:
  1. `Din rabat er aktiveret — de næste {durationMonths} måneder fortsætter dit abonnement til rabatpris.`
  2. `Efter de {durationMonths} måneder fortsætter dit abonnement til den normale pris.`
- Info box (sand, rounded-2xl, p-5) title "Din rabat:" with 3 items (♡ prefix):
  - `"20% i {durationMonths} måneder"`
  - "Ingen handling nødvendig"
  - "Fortsætter automatisk efterfølgende"

All three variants share a single primary CTA at the bottom: "Tilbage til forsiden" → `onReturn()`.

Copy must match research doc verbatim. Any paraphrase is a bug. All headings use `font-serif text-2xl sm:text-3xl`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>All 7 files exist, tsc --noEmit exits 0, exact Danish strings from RESEARCH.md §7 present (spot-checked via grep for a distinctive phrase in each file).</done>
</task>

<task type="auto">
  <name>Task 2: Wire all step components into cancel-flow.tsx, remove placeholder + hidden span</name>
  <files>app/dashboard/subscription/cancel/_components/cancel-flow.tsx</files>
  <action>
Edit `cancel-flow.tsx`:

1. Add imports at top for all 7 new step components.

2. Delete the entire placeholder block `{(step === 'offer-discount' || ...) && <div>TODO Plan 12-03...`

3. Delete the hidden `<span className="hidden" aria-hidden>` hack.

4. After the existing `{step === 'reason' && <StepReason .../>}` block, add the following conditional renders in order:

```tsx
{step === 'offer-discount' && offerConfig && (
  <StepOfferDiscount
    offerConfig={offerConfig}
    priceAmountCents={props.priceAmountCents}
    loading={isPending}
    onBack={() => setStep('reason')}
    onAccept={() => handleAcceptOffer()}
    onDecline={() => setStep('confirm')}
  />
)}

{step === 'offer-pause' && (
  <StepOfferPause
    loading={isPending}
    onBack={() => setStep('reason')}
    onAccept={(months) => handleAcceptOffer(months)}
    onDecline={() => setStep('confirm')}
  />
)}

{step === 'offer-support' && offerConfig && (
  <StepOfferSupport
    offerConfig={offerConfig}
    onBack={() => setStep('reason')}
    onDecline={() => setStep('confirm')}
  />
)}

{step === 'offer-content' && (
  <StepOfferContent
    onBack={() => setStep('reason')}
    onStay={handleReturnToDashboard}
    onDecline={() => setStep('confirm')}
  />
)}

{step === 'offer-skip' && (
  <StepOfferSkip
    onBack={() => setStep('reason')}
    onContinue={() => setStep('confirm')}
  />
)}

{step === 'confirm' && (
  <StepConfirm
    periodEndDate={periodEndDate}
    loading={isPending}
    onBack={() => setStep('reason')}
    onConfirm={handleConfirmCancel}
    onGoBack={handleReturnToDashboard}
  />
)}

{(step === 'done-cancel' || step === 'done-pause' || step === 'done-retention') && (
  <StepDone
    variant={step === 'done-cancel' ? 'cancel' : step === 'done-pause' ? 'pause' : 'retention'}
    userName={props.userName}
    periodEndDate={periodEndDate}
    expiresAt={acceptanceExpiresAt}
    offerConfig={offerConfig}
    onReturn={handleReturnToDashboard}
  />
)}
```

5. Make sure all previously "unused" local variables (handleConfirmCancel, handleAcceptOffer, acceptanceExpiresAt, offerConfig, periodEndDate, props.userName, props.priceAmountCents) are now genuinely referenced — remove the hidden-span hack entirely.

6. For CONTENT_HELP: note that `onStay={handleReturnToDashboard}` just routes back to /dashboard without calling acceptOfferAction. This is intentional for Phase 12 — CONTENT_HELP has no Stripe side-effect and the survey already captured the user's reason. If an offer row was returned by resolveEligibleOffer, no acceptance is recorded in Phase 12 for CONTENT variant. Add a comment: `// Phase 12 decision: CONTENT_HELP "stay" does not create an acceptance row — survey alone captures the reason.`

Addresses OFF-UI-04, OFF-UI-05, OFF-UI-06.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>cancel-flow.tsx imports and renders all 7 step components based on step state; the TODO placeholder block and hidden-span hack are both removed; tsc --noEmit exits 0.</done>
</task>

<task type="auto">
  <name>Task 3: Create VALIDATION.md documenting Phase 12 UI verification strategy</name>
  <files>.planning/phases/12-hygge-cancel-flow-ui/VALIDATION.md</files>
  <action>
Create `.planning/phases/12-hygge-cancel-flow-ui/VALIDATION.md` with the following content:

```markdown
# Phase 12 — Hygge Cancel Flow UI — Validation

**Phase type:** Pure UI (no new service logic; all business logic already tested in Phases 10 and 11)

## Verification Strategy

Phase 12 is verified by two gates:

### Gate 1: TypeScript compile (automated, per-task)
- Command: `npx tsc --noEmit`
- Must exit 0 after every task commit.
- Covers: type safety of server action contracts, EligibleOffer prop shapes, FlowStep discriminated unions, and all step component prop interfaces.
- Rationale: Phase 10 and 11 Vitest suites already cover `cancelSubscription`, `resolveEligibleOffer`, and `acceptOffer`. Phase 12 adds only UI glue — the services it calls are proven green.

### Gate 2: Full Vitest suite (automated, per-plan merge)
- Command: `npx vitest run`
- Must exit 0 at the end of each plan.
- Confirms Phase 10/11 tests still pass after any indirect changes (schema updates, import graph, etc).

### Gate 3: Manual browser walkthrough (human, pre-phase-complete)
Manual browser verification is required because:
- Hygge tone and empathetic copy cannot be checked by TypeScript
- Mobile responsiveness at 375px is visual
- Step-to-step state transitions are interaction-based

**Manual test plan (run after all 3 plans merged):**

1. Visit `/dashboard/settings` as a user with an active entitlement → verify "Opsig abonnement" link appears under ManageSubscriptionButton.
2. Click it → verify Step 1 renders with 🌿 illustration and heading "Vi er kede af at se dig gå".
3. **Happy path (reason → offer → accept):**
   - Click "Ja, hjælp gerne" → Step 2 reason picker visible.
   - Select "Prisen" chip → "Næste" enables.
   - Click Næste → Step 3 discount variant should show (assumes a DISCOUNT offer is seeded for `pris` reason; if not, any configured offer type is acceptable as long as routing works).
   - Click "Ja tak, det vil jeg gerne" → Step 5 retention variant with 🎁.
   - Click "Tilbage til forsiden" → lands on /dashboard.
4. **Skip path:**
   - Revisit /dashboard/subscription/cancel → click "Nej tak, jeg vil bare opsige" → jumps directly to Step 4 confirm.
   - Click "Opsig abonnement" → Step 5 cancel variant with 🏡.
5. **Pause path:**
   - Revisit flow, select "For lidt tid" → Step 3 pause variant (assuming PAUSE offer seeded for `tid`).
   - Click "2 måneder" button → "Pause mit abonnement" enables.
   - Click it → Step 5 pause variant with ⏸️ and resumesAt date shown.
6. **Decline offer path:**
   - Revisit flow, pick any reason → Step 3 → click the decline link → Step 4 confirm appears.
   - Click "Nej, jeg vil beholde mit abonnement" → returns to /dashboard without cancelling.
7. **Mobile layout:**
   - DevTools responsive mode at 375px width. All 10 step screens render without horizontal scroll. All buttons ≥44px tall.
8. **Design system:**
   - Sand background visible behind the card column.
   - Coral primary buttons (#E8715A).
   - DM Serif Display on all headings.

## Requirement Coverage

| Req ID | How Verified |
|--------|-------------|
| OFF-UI-01 | Manual: "Opsig abonnement" link visible on settings page |
| OFF-UI-02 | Manual: Step 1 renders empathetic copy + skip link → confirm |
| OFF-UI-03 | Manual: Chips render from DB, multi-select works, textarea optional |
| OFF-UI-04 | Manual: All 5 offer variants render when routed from Step 2 |
| OFF-UI-05 | Manual: Step 4 confirm shows periodEndDate; clicking "Opsig" triggers cancelSubscription + Step 5 cancel |
| OFF-UI-06 | Manual: Step 5 renders cancel / pause / retention variants with personalized copy |
| OFF-UI-07 | Manual: 375px mobile viewport renders correctly, no horizontal overflow |

## Known Gaps

- No Playwright E2E tests added in Phase 12. A future phase may add one full happy-path Playwright test if regression risk grows. Current rationale: service layer is tested, UI is simple state machine, manual walkthrough catches copy and visual regressions that tests would miss anyway.
- CONTENT_HELP "Fortsæt mit abonnement" does not create a RetentionOfferAcceptance row — Phase 13 decision if we need tracking.
- DISCOUNT "newPrice" is derived client-side (`priceAmountCents * 0.8`). If the Stripe coupon uses a percentage other than 20%, the displayed price will be wrong. Phase 13 should read coupon percent from offer config.

## Sign-off

Phase 12 is considered complete when:
- [ ] All 3 plans merged (12-01, 12-02, 12-03)
- [ ] `npx tsc --noEmit` exits 0
- [ ] `npx vitest run` exits 0
- [ ] Manual browser walkthrough completed and approved by user
```
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>VALIDATION.md exists at the phase directory, documents both automated gates and the manual walkthrough, maps all 7 OFF-UI-* requirements.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0
- `npx vitest run` exits 0 (confirms Phase 10/11 tests still pass)
- `grep -l "Tak for din tid hos os" app/dashboard/subscription/cancel/_components/step-done.tsx`
- `grep -l "Vil du have en pause i stedet" app/dashboard/subscription/cancel/_components/step-offer-pause.tsx`
- `grep -l "StepOfferDiscount" app/dashboard/subscription/cancel/_components/cancel-flow.tsx`
- `test -f .planning/phases/12-hygge-cancel-flow-ui/VALIDATION.md`
- Placeholder text "TODO Plan 12-03" is NOT present in cancel-flow.tsx
</verification>

<success_criteria>
Full cancel flow works end-to-end: settings → intro → reason → offer (1 of 5 variants) → confirm or direct accept → done (1 of 3 variants) → /dashboard. All copy matches mockup verbatim. All TypeScript compiles cleanly. VALIDATION.md documents the manual browser walkthrough.
</success_criteria>

<output>
Create `.planning/phases/12-hygge-cancel-flow-ui/12-03-SUMMARY.md` per template.
</output>
