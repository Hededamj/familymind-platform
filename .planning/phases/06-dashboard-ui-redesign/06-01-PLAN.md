---
phase: 06-dashboard-ui-redesign
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/dashboard/_components/section-heading.tsx
  - app/dashboard/_components/dashboard-check-in.tsx
  - app/dashboard/actions.ts
autonomous: true
requirements: [CHECKIN-02, VISUAL-02, VISUAL-03]

must_haves:
  truths:
    - "User sees a visually prominent check-in container on the dashboard with the contextual prompt"
    - "User can type free-text reflection and submit it without leaving the dashboard"
    - "After submitting, the check-in container shows a confirmation and disables re-submission"
    - "Dashboard sections have serif headings with muted secondary text beneath"
  artifacts:
    - path: "app/dashboard/actions.ts"
      provides: "submitDashboardReflection server action"
      exports: ["submitDashboardReflection"]
    - path: "app/dashboard/_components/dashboard-check-in.tsx"
      provides: "Client component with highlighted container, prompt text, textarea, and submit button"
      contains: "use client"
    - path: "app/dashboard/_components/section-heading.tsx"
      provides: "Reusable section heading with title and subtitle"
      exports: ["SectionHeading"]
  key_links:
    - from: "app/dashboard/_components/dashboard-check-in.tsx"
      to: "app/dashboard/actions.ts"
      via: "submitDashboardReflection server action call"
      pattern: "submitDashboardReflection"
    - from: "app/dashboard/_components/dashboard-check-in.tsx"
      to: "prisma.userDayCheckIn"
      via: "server action creates check-in record"
      pattern: "userDayCheckIn.create"
---

<objective>
Create the dashboard check-in component (free-text reflection with visually highlighted container) and a reusable section heading component.

Purpose: Users need to write reflections directly from the dashboard (CHECKIN-02) in a visually distinct container (VISUAL-02), and all dashboard sections need clear headings with secondary text (VISUAL-03).

Output: Three files — a server action, a client check-in component, and a section heading component — ready for Plan 02 to wire into the restructured dashboard page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dashboard-service-layer/05-01-SUMMARY.md
@.planning/phases/05-dashboard-service-layer/05-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From lib/services/dashboard.service.ts:
```typescript
export type DashboardState =
  | 'new_user'
  | 'active_journey'
  | 'active_journey_plus_courses'
  | 'no_journey_has_courses'
  | 'completed_journey'

export async function getCheckInPrompt(userId: string): Promise<string>
// getDashboardState returns: { checkInPrompt: string, weeklyFocus, personalizedWelcome, ... }
```

From prisma/schema.prisma:
```prisma
model UserDayCheckIn {
  id              String   @id @default(uuid()) @db.Uuid
  userJourneyId   String   @db.Uuid
  dayId           String   @db.Uuid
  checkInOptionId String   @db.Uuid
  reflection      String?
  completedAt     DateTime @default(now())
}

model CheckInOption {
  id       String  @id @default(uuid()) @db.Uuid
  label    String
  value    String
  emoji    String?
  position Int
  isActive Boolean @default(true)
}
```

From app/journeys/[slug]/actions.ts (existing pattern for server actions):
```typescript
export async function completeDayAction(
  userJourneyId: string, dayId: string,
  checkInOptionId: string, reflection?: string, journeySlug?: string
)
```

Existing check-in form pattern from app/journeys/[slug]/_components/check-in-form.tsx:
- Uses CheckInOption emoji buttons (grid-cols-2 gap-3 sm:grid-cols-4)
- Has Textarea from @/components/ui/textarea for reflection
- Uses sticky submit button with safe-area-inset pattern
- Danish labels throughout

Design system colors (from app/globals.css):
- --color-sand: #F5F0EB (warm background)
- --color-sand-dark: #EDE6DF (darker warm)
- --color-coral: #E8715A (accent, used sparingly)
- --color-coral-light: rgba(232, 113, 90, 0.1)
- --primary: #86A0A6 (Nordic blue-green)
- font-serif class used for headings throughout app
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server action for dashboard reflection + SectionHeading component</name>
  <files>app/dashboard/actions.ts, app/dashboard/_components/section-heading.tsx</files>
  <read_first>
    - app/journeys/[slug]/actions.ts (existing server action pattern with requireAuth + IDOR check)
    - prisma/schema.prisma (UserDayCheckIn model, CheckInOption model)
    - lib/services/dashboard.service.ts (getUserActiveJourney import path)
  </read_first>
  <action>
Create two files:

**app/dashboard/actions.ts** — Server action `submitDashboardReflection`:
- 'use server' at top
- Import `requireAuth` from `@/lib/auth`, `prisma` from `@/lib/prisma`, `revalidatePath` from `next/cache`, `getUserActiveJourney` from `@/lib/services/journey.service`
- Function signature: `export async function submitDashboardReflection(reflection: string)`
- Validate: `reflection.trim()` must be non-empty, throw if empty
- Call `requireAuth()` to get user
- Call `getUserActiveJourney(user.id)` to get active journey
- If no active journey or no currentDayId: return `{ error: 'Ingen aktiv dag at registrere refleksion for.' }`
- Look up the first active CheckInOption: `prisma.checkInOption.findFirst({ where: { isActive: true }, orderBy: { position: 'asc' } })` — dashboard reflections use the default option since user is just writing text, not selecting a mood
- If no checkInOption found: return `{ error: 'Ingen check-in muligheder konfigureret.' }`
- Create the check-in: `prisma.userDayCheckIn.create({ data: { userJourneyId: activeJourney.id, dayId: activeJourney.currentDayId, checkInOptionId: checkInOption.id, reflection: reflection.trim() } })`
- Call `revalidatePath('/dashboard')`
- Return `{ success: true }`

**app/dashboard/_components/section-heading.tsx** — Reusable heading (per VISUAL-03):
- Server component (no 'use client')
- Props: `{ title: string; subtitle?: string }`
- Render: `<div className="mb-4">` containing `<h2 className="font-serif text-xl">` for title, and if subtitle: `<p className="mt-0.5 text-sm text-muted-foreground">` for subtitle
- Export as named export `SectionHeading`
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>
    - actions.ts exports submitDashboardReflection with auth, validation, and prisma create
    - SectionHeading renders serif heading + optional muted subtitle
    - Both files pass TypeScript compilation
  </done>
</task>

<task type="auto">
  <name>Task 2: Dashboard check-in client component with highlighted container</name>
  <files>app/dashboard/_components/dashboard-check-in.tsx</files>
  <read_first>
    - app/journeys/[slug]/_components/check-in-form.tsx (existing check-in UI pattern — emoji buttons, textarea, submit flow)
    - app/dashboard/actions.ts (the server action just created in Task 1)
    - components/ui/textarea.tsx (Textarea import)
    - components/ui/button.tsx (Button import)
  </read_first>
  <action>
Create `app/dashboard/_components/dashboard-check-in.tsx` — a 'use client' component implementing CHECKIN-02 and VISUAL-02.

**Props interface:**
```typescript
interface DashboardCheckInProps {
  prompt: string        // from getDashboardState().checkInPrompt
  hasActiveDay: boolean // whether user has an active journey day (controls if submit is possible)
}
```

**Component structure:**
- Outer container: `<div className="overflow-hidden rounded-2xl bg-[var(--color-sand-dark)] p-5 sm:p-6">` — this is the visually highlighted container (VISUAL-02), using --color-sand-dark for a warm, prominent background that stands out from the page background. Add a left border accent: `border-l-4 border-[var(--color-coral)]` for visual pop similar to competitor's golden box approach.
- Display the `prompt` as: `<p className="font-serif text-lg leading-snug">{prompt}</p>`
- Below prompt, a `<Textarea>` from `@/components/ui/textarea` with:
  - `placeholder="Skriv din refleksion her..."` 
  - `rows={3}`
  - `className="mt-3 resize-none border-[var(--color-sand-dark)] bg-white/80 focus:bg-white"`
  - Controlled by `useState<string>('')`
  - `maxLength={1000}`
- Below textarea, a submit `<Button>` with:
  - `className="mt-3 w-full rounded-xl min-h-[44px]"` (44px touch target per project convention)
  - `size="lg"`
  - Text: "Gem refleksion"
  - Disabled when: `isSubmitting || !reflection.trim() || !hasActiveDay`
  - Shows `<Loader2 className="mr-2 size-4 animate-spin" />` + "Gemmer..." when submitting
- If `!hasActiveDay`: show a muted note below the prompt instead of the textarea: `<p className="mt-2 text-sm text-muted-foreground">Start et forløb for at skrive daglige refleksioner.</p>` — do NOT render textarea or button.

**Submit flow:**
- `useState` for `reflection`, `isSubmitting`, `submitted`
- On submit: call `submitDashboardReflection(reflection)` imported from `../actions`
- On success: set `submitted = true`, show confirmation state
- On error: show error via `<p className="text-sm text-destructive">` 

**Confirmation state** (when `submitted === true`):
- Replace textarea+button with: `<div className="mt-3 flex items-center gap-2 text-sm text-[var(--color-success)]"><Check className="size-4" /> Din refleksion er gemt</div>`
- Import `Check` from lucide-react

**Imports needed:** useState from react, Button from @/components/ui/button, Textarea from @/components/ui/textarea, Loader2 and Check from lucide-react, submitDashboardReflection from ../actions
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>
    - DashboardCheckIn renders highlighted sand-dark container with coral left border
    - Prompt text displayed in serif font
    - Textarea and submit button shown only when hasActiveDay is true
    - Submitting calls server action and shows confirmation
    - Component passes TypeScript compilation
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes without errors
- All three new files exist and export their named components/functions
- DashboardCheckIn uses 'use client' directive
- SectionHeading does NOT use 'use client' (server component)
- actions.ts uses 'use server' directive
</verification>

<success_criteria>
- CHECKIN-02: submitDashboardReflection action creates UserDayCheckIn with reflection text, DashboardCheckIn component provides the UI
- VISUAL-02: Check-in container uses --color-sand-dark background + coral left border, visually distinct from surrounding content
- VISUAL-03: SectionHeading component renders font-serif title + text-muted-foreground subtitle, ready for use in all dashboard sections
</success_criteria>

<output>
After completion, create `.planning/phases/06-dashboard-ui-redesign/06-01-SUMMARY.md`
</output>
