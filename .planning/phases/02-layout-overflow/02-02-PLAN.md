---
phase: 02-layout-overflow
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/dashboard/progress/page.tsx
autonomous: true
requirements: [LAYOUT-02, LAYOUT-03]

must_haves:
  truths:
    - "Progress page stat grid shows single column on screens under 640px"
    - "Progress page monthly stat breakdown is readable on narrow screens"
    - "All user-facing pages have consistent max-width containers"
  artifacts:
    - path: "app/dashboard/progress/page.tsx"
      provides: "Responsive stat grid that collapses on mobile"
      contains: "sm:grid-cols-2"
  key_links:
    - from: "app/dashboard/progress/page.tsx"
      to: "StatCard component"
      via: "grid layout"
      pattern: "sm:grid-cols"
---

<objective>
Fix grid collapse behavior on the progress page so all grids respond correctly to narrow screens.

Purpose: Grids must collapse to single column on screens under 640px (LAYOUT-03). Also verify consistent max-width across all pages (LAYOUT-02) -- most already done, this plan confirms the remaining pages.
Output: Progress page with responsive grids.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@app/dashboard/progress/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix progress page stat grid and monthly breakdown grid</name>
  <files>app/dashboard/progress/page.tsx</files>
  <read_first>app/dashboard/progress/page.tsx</read_first>
  <action>
1. Fix the all-time stats grid on line 44. Change:
   `className="mb-8 grid grid-cols-2 gap-3 sm:grid-cols-3"`
   to:
   `className="mb-8 grid grid-cols-1 gap-3 sm:grid-cols-2 md:grid-cols-3"`
   This ensures single column on mobile (<640px), 2 cols on sm, 3 cols on md+.

2. Fix the monthly breakdown inner stat grid on line 148. Change:
   `className="grid grid-cols-3 gap-2 text-center"`
   to:
   `className="grid grid-cols-3 gap-2 text-center"`
   Actually this 3-col grid for the 3 monthly stats (Indhold, Check-ins, Dage aktiv) is fine at 3 cols -- the cells are small numeric values with short labels. Each cell only shows a number and a single word. Leave as-is.

   However, verify the page wrapper and container are correct:
   - Line 25: `className="px-4 py-8 sm:px-8"` -- has padding, good
   - Line 26: `className="mx-auto w-full max-w-2xl"` -- has max-width, good (LAYOUT-02 confirmed)

3. Also add `overflow-hidden` to the milestone item container on line 99 to prevent long milestone titles from breaking layout. Change:
   `className="flex items-start gap-4 rounded-2xl border border-border bg-white p-5"`
   to:
   `className="flex items-start gap-4 overflow-hidden rounded-2xl border border-border bg-white p-5"`

Summary of LAYOUT-02 verification (all confirmed in codebase):
- dashboard/page.tsx: max-w-2xl (done)
- browse/page.tsx: max-w-4xl (done)
- community/page.tsx: max-w-3xl (done)
- community/[roomSlug]/page.tsx: max-w-3xl (done)
- journeys/[slug]/page.tsx: max-w-2xl (done)
- content/[slug]/page.tsx: max-w-3xl (done)
- dashboard/progress/page.tsx: max-w-2xl (done)
All pages have consistent max-width. LAYOUT-02 is satisfied.
  </action>
  <verify>
    <automated>cd "C:/Users/jacob.hummel/Claude/familymind-platform" && grep -q "grid-cols-1" app/dashboard/progress/page.tsx && grep -q "sm:grid-cols-2" app/dashboard/progress/page.tsx && grep -q "md:grid-cols-3" app/dashboard/progress/page.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - progress/page.tsx stat grid has "grid-cols-1 ... sm:grid-cols-2 md:grid-cols-3"
    - progress/page.tsx milestone items have "overflow-hidden"
    - All user-facing pages have max-width containers (already verified: max-w-2xl, max-w-3xl, or max-w-4xl)
  </acceptance_criteria>
  <done>Progress page stat grid collapses to single column on mobile; all pages have consistent max-width</done>
</task>

</tasks>

<verification>
All grids collapse to single column on narrow screens. All pages have consistent max-width.

```bash
# Verify grid fix
grep "grid-cols-1" app/dashboard/progress/page.tsx
grep "sm:grid-cols-2" app/dashboard/progress/page.tsx

# Verify max-width across all pages
grep -r "max-w-" app/dashboard/page.tsx app/browse/page.tsx app/community/page.tsx app/dashboard/progress/page.tsx
```
</verification>

<success_criteria>
- Progress stat grid shows 1 column on mobile, 2 on sm, 3 on md+
- All user-facing pages (dashboard, browse, community, journeys, content, progress) have max-width containers
- No grid shows multi-column layout below 640px width (except the 3-cell monthly stat summary which contains only small numbers)
</success_criteria>

<output>
After completion, create `.planning/phases/02-layout-overflow/02-02-SUMMARY.md`
</output>
